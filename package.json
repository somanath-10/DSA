{
  "name": "dsa-progress-tracker",
  "version": "1.0.0",
  "private": true,
  "description": "Full-stack React + Node tracker for company-wise DSA questions.",
  "workspaces": ["client", "server"],
  "scripts": {
    "dev": "concurrently \"npm run dev --workspace server\" \"npm run dev --workspace client\"",
    "start": "npm run start --workspace server",
    "check": "npm run check --workspace server",
    "reset-progress": "npm run reset-progress --workspace server"
  },
  "devDependencies": {
    "concurrently": "^8.2.2"
  }
}
